# Optional absolute API origin for Vite.
# Leave this empty when the production frontend is served by nginx and proxies /api to the backend.
VITE_API_ORIGIN=

# Optional application environment label available at build time.
VITE_APP_ENV=production
